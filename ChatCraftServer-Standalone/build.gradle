group 'ChatCraftServer'
version '1.0-SNAPSHOT'

buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'com.github.jengelman.gradle.plugins:shadow:1.2.3'
    }
}

apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'com.github.johnrengelman.shadow'

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

repositories {
    mavenCentral()
}

dependencies {
    compile rootProject
}

jar {
    manifest {
        attributes 'Main-Class': 'com.nearce.chatcraft.standalone.ChatCraftStandalone'
    }
}

shadowJar {
    dependencies {
        include(dependency(rootProject))
        include(dependency('com.google.code.gson:gson:2.6.2'))
        include(dependency('org.java-websocket:Java-WebSocket:1.3.0'))
    }

    relocate('com.google.gson', 'com.nearce.chatcraft.standalone.internal.gson')
    relocate('org.java-websocket', 'com.nearce.chatcraft.standalone.internal.websocket')

}

build.dependsOn(shadowJar)